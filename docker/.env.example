# Chave de API global da Evolution API — troque em produção
EVOLUTION_API_KEY=dev-evolution-key-change-me

# URL que a Evolution vai chamar pro webhook do seu backend
# Em dev local com Docker: use host.docker.internal
# Em produção: use a URL pública do seu backend
WEBHOOK_URL=http://host.docker.internal:3001/webhooks/evolution
